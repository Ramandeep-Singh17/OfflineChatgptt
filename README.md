# OfflineChatgptt
I built an offline ChatGPT-like system using transfer learning on a Hugging Face model. The system does not rely on any external APIs or API keys; instead, it is entirely self-hosted and driven by transfer learning and prompt engineering.
